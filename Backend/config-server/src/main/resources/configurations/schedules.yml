eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8091
spring:
  application:
    name: schedules
  datasource:
    url: jdbc:mariadb://localhost:3306/springboot_mariadb?createDatabaseIfNotExist=true
    username: root
    password: 1007
  jpa:
    hibernate:
      ddl-auto: update