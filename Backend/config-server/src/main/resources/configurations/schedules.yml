eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8091
spring:
  application:
    name: schedules
  datasource:
    url: jdbc:mariadb://localhost:3306/schedule?createDatabaseIfNotExist=True
    username: root
    password: 123
  jpa:
    hibernate:
      ddl-auto: update